import React from "react"
import { Link, useNavigate } from "react-router-dom"

function FooterMd() {
	const navigate = useNavigate()

	return <footer>{/* <Link to="/about">About</Link> */}</footer>
}

export default FooterMd
